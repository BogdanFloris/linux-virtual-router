#define MAX_NAME_LEN                                                           \
    64 // Max length for general names (namespace, bridge, firewall zones)
#define MAX_IF_NAME_LEN                                                        \
    16 // Max length specifically for OS interface names (like ens160, eth0)
